export class Product{
    constructor(public productId: number, public productName: string, public unitPrice: number, public categoryId: number,
         public categoryName: string, public supplierId: number, public supplierName: string){

    }
}